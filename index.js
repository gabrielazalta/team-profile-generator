//logic

const { start } = require("repl");

function start(){
//everyone has an email, id, name, role
//if role == manager
    //manager(ans.email, ans.id, ans.name)
}

function manager(name, email, roomnum){
//ask for roomnumber
//ask if they are done
//what do we do?
//create the webpage with the manager (how you will design will be like readmegen function with filable vars (m.email , m.getRole()))
//if you are not done, then we ask for startfx start()
}